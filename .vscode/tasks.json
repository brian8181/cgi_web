{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "MAKE DEBUGGDB",
            "type": "shell",
            "command": "make -f ${workspaceFolder}/cgi/Makefile",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "MAKE index",
            "type": "shell",
            "command": "make -f ${workspaceFolder}/cgi/Makefile ${workspaceFolder}/cgi/index.cgi",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "echo",
            "type": "shell",
            "command": "echo ${workspaceFolder}"
        }
    ]
}